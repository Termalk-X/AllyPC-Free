name: Windows Cloud PC
on:
  workflow_dispatch:
jobs:
  windows:
    runs-on: windows-latest
    steps:
      - name: Show Windows information
        run: |
          systeminfo
          echo "Windows runner aktif!"
